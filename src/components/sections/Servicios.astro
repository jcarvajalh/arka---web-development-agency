---
// ServicesCarousel.astro

const services = [
  {
    number: "01",
    title: "Diseño Personalizado",
    description:
      "Interfaces únicas diseñadas para tu marca. Cada pixel pensado para guiar a tu visitante hacia la acción. Versión desktop y mobile perfectamente adaptadas.",
    tags: ["UI/UX", "Responsive", "Figma"],
    image: "../public/diseño-personalizado.webp",
    alt: "Diseño Personalizado",
  },
  {
    number: "02",
    title: "SEO & Conversiones",
    description:
      "No solo creamos sitios bonitos. Implementamos SEO en cada página y estructuramos el contenido para alcanzar tasas de conversión ideales.",
    tags: ["SEO Técnico", "CRO", "Analytics"],
    image: "../public/seo-cali.webp",
    alt: "SEO y Conversiones",
  },
  {
    number: "03",
    title: "Inteligencia Artificial",
    description:
      "Integramos herramientas de inteligencia artificial que automatizan procesos: Sistemas inteligentes, y análisis predictivo de comportamiento.",
    tags: ["AI Agents", "Automatización"],
    image: "../public/inteligencia-artificial.webp",
    alt: "Inteligencia Artificial",
  },
  {
    number: "04",
    title: "Tecnologias modernas",
    description:
      "Construimos sitios web de alto rendimiento con tecnologías modernas. Velocidad, seguridad y escalabilidad para que tu negocio crezca sin límites técnicos.",
    tags: ["Astro", "Next.js", "Shopify"],
    image: "../public/tecnologias.webp",
    alt: "Tecnologias modernas",
  },
];
---

<section class="services-section">
  <!-- Header -->
  <div class="services-header">
    <p class="section-label">Nuestros Servicios</p>
    <h2 class="section-title">
      Todo lo que tu negocio necesita para crecer online
    </h2>
    <p class="section-sub">
      Cuatro pilares estratégicos que convierten tu sitio web en una máquina de
      ventas.
    </p>
  </div>

  <!-- Carousel -->
  <div class="carousel-outer">
    <div class="carousel-track" id="servicesTrack">
      {
        [...services, ...services].map((service, i) => (
          <div
            class="service-card"
            aria-hidden={i >= services.length ? "true" : undefined}
          >
            <div class="card-image">
              <img
                src={service.image}
                alt={service.alt}
                loading="lazy"
                class="card-img"
              />
              <div class="card-gradient" />
            </div>

            <div class="card-body">
              <div class="card-number">{service.number}</div>
              <h3 class="card-title">{service.title}</h3>
              <p class="card-desc">{service.description}</p>
              <div class="tags">
                {service.tags.map((tag) => (
                  <span class="tag">{tag}</span>
                ))}
              </div>
            </div>
          </div>
        ))
      }
    </div>
  </div>

  <!-- Controls -->
  <div class="carousel-controls">
    <button class="nav-btn" id="prevBtn" aria-label="Anterior">
      <svg
        width="14"
        height="14"
        viewBox="0 0 24 24"
        fill="none"
        stroke="currentColor"
        stroke-width="2.5"
      >
        <path d="M15 19l-7-7 7-7"></path>
      </svg>
    </button>
    <button class="nav-btn" id="nextBtn" aria-label="Siguiente">
      <svg
        width="14"
        height="14"
        viewBox="0 0 24 24"
        fill="none"
        stroke="currentColor"
        stroke-width="2.5"
      >
        <path d="M9 5l7 7-7 7"></path>
      </svg>
    </button>
  </div>
</section>

<script>
  // DOMContentLoaded garantiza que el DOM esté listo antes de buscar los elementos
  document.addEventListener("DOMContentLoaded", () => {
    const track = document.getElementById(
      "servicesTrack",
    ) as HTMLElement | null;
    const prevBtn = document.getElementById(
      "prevBtn",
    ) as HTMLButtonElement | null;
    const nextBtn = document.getElementById(
      "nextBtn",
    ) as HTMLButtonElement | null;

    if (!track || !prevBtn || !nextBtn) return;

    const GAP = 20;
    const allCards = Array.from(
      track.querySelectorAll<HTMLElement>(".service-card"),
    );
    const total = allCards.length / 2; // la segunda mitad son clones

    let currentIndex = 0;
    let isTransitioning = false;

    /* ── Imágenes: añade .loaded cuando cargan, silencia el error de consola ── */
    track.querySelectorAll<HTMLImageElement>(".card-img").forEach((img) => {
      // Si ya cargó (caché)
      if (img.complete && img.naturalWidth > 0) {
        img.classList.add("loaded");
      } else {
        img.addEventListener("load", () => img.classList.add("loaded"));
        img.addEventListener("error", () => {
          /* no hacer nada; placeholder queda visible */
        });
      }
    });

    /* ── Helpers ── */
    function getCardWidth(): number {
      return allCards[0].offsetWidth + GAP;
    }

    function setPosition(index: number, animate: boolean) {
      track.style.transition = animate
        ? "transform 0.55s cubic-bezier(0.25, 0.46, 0.45, 0.94)"
        : "none";
      track.style.transform = `translateX(-${index * getCardWidth()}px)`;
    }

    function goNext() {
      if (isTransitioning) return;
      isTransitioning = true;
      currentIndex++;
      setPosition(currentIndex, true);
    }

    function goPrev() {
      if (isTransitioning) return;
      isTransitioning = true;
      currentIndex--;
      setPosition(currentIndex, true);
    }

    /* ── Loop infinito ──
       Cuando llega al clon del primer elemento (index === total)
       salta silenciosamente al original sin animación, y viceversa. */
    track.addEventListener("transitionend", () => {
      isTransitioning = false;

      if (currentIndex >= total) {
        currentIndex -= total;
        setPosition(currentIndex, false);
      } else if (currentIndex < 0) {
        currentIndex += total;
        setPosition(currentIndex, false);
      }
    });

    nextBtn.addEventListener("click", goNext);
    prevBtn.addEventListener("click", goPrev);

    /* ── Drag / Swipe ── */
    let startX = 0;
    let dragging = false;

    track.addEventListener("mousedown", (e) => {
      dragging = true;
      startX = e.clientX;
    });
    track.addEventListener("mouseup", (e) => {
      if (!dragging) return;
      dragging = false;
      const diff = startX - e.clientX;
      if (Math.abs(diff) > 50) diff > 0 ? goNext() : goPrev();
    });
    track.addEventListener("mouseleave", () => {
      dragging = false;
    });

    track.addEventListener(
      "touchstart",
      (e) => {
        startX = e.touches[0].clientX;
      },
      { passive: true },
    );
    track.addEventListener("touchend", (e) => {
      const diff = startX - e.changedTouches[0].clientX;
      if (Math.abs(diff) > 50) diff > 0 ? goNext() : goPrev();
    });

    /* ── Resize ── */
    window.addEventListener("resize", () => setPosition(currentIndex, false));
  });
</script>
